"""Web search tool placeholder."""

from __future__ import annotations


def run(query: str) -> dict[str, object]:
    return {
        "query": query,
        "results": [],
        "note": "Web integration is intentionally stubbed in scaffold phase.",
    }
