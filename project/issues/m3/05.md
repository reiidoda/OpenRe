# [M3] Implement best-config registry

## Summary
Persist selected best configurations per dataset with versioned metadata.

## Acceptance Criteria
- Registry stores config id, score breakdown, and timestamp.
- CLI command prints current best config for dataset.
- Registry update is atomic and test-covered.

## Labels
- milestone:M3
- type:feature
- priority:P1
