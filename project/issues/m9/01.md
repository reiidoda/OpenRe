# [M9] Deliver canonical OpenRe CLI command surface

## Summary
Implement the canonical `openre` CLI command set with stable UX and backward compatibility aliases.

## Acceptance Criteria
- Commands implemented: `init`, `test`, `run`, `compare`, `trace`, `eval`, `approve`, `report`, `leaderboard`.
- Help output and command contracts are documented and tested.
- Existing `awb` alias remains functional during migration window.

## Labels
- milestone:M9
- type:feature
- priority:P0
