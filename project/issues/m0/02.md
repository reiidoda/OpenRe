# [M0] Implement baseline CLI commands

## Summary
Add CLI entrypoint with command surface for run/compare/eval/optimize/approve/report.

## Acceptance Criteria
- `awb --help` shows all v1 commands.
- `awb run` executes local scaffold flow.
- Command payloads are emitted as JSON for scripting.

## Labels
- milestone:M0
- type:feature
- priority:P0
