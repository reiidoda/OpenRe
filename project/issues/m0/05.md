# [M0] Configure CI and pre-commit baseline

## Summary
Add GitHub workflows and pre-commit checks for lint, type-check, and tests.

## Acceptance Criteria
- CI workflow runs on pull requests.
- Pre-commit config includes lint + whitespace + yaml checks.
- `make check` succeeds locally.

## Labels
- milestone:M0
- type:infra
- priority:P1
